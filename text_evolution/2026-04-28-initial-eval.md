# 演化记录 · 2026-04-28-初次评测 (initial-eval)

## 任务上下文
- 时间：2026-04-28
- 项目阶段：skill 自身评测（4 轮 + 中途场景测试）
- 用户 prompt：测试 skill 质量、性能、效率，以及在中途接手项目时的表现

## 触发情况
- skill 是否触发：✅（subagent 强制加载形式）
- 跳过条件是否生效：✅（v0.4 final 的 Test 1 触发了"先写最小可运行版本"）
- 主导姿态：覆盖 5 种姿态各类场景

## 心法引用
- 入场两问是否走完：✅ 多数场景走完；少数（Test 1 v0.4 final）触发跳过条件
- 引用的禁令编号：**#1（最高频）**、#2、#4、**#5（演化主线）**、#6
- 引用的姿态条款：止（多次）、变（M2 清理 try/except）、复（M3 debug 偏离）

## 输出效果
- 自评：**好**
- 是否产出可运行代码：Test 1 v0.4 final ✅，其他场景多为"要事实再动"
- 用户反馈：v0.1 自相矛盾（禁令 #5 vs 止）→ v0.2 修好；description 加了"加 try/except 加配置加抽象诱惑" → trigger F1 87.5% → 100%（多数票）

## 盲点 / 改进建议
- **#1 边界灰色**："简单 PDF 拆页工具"在评测员眼里 50/50 分裂——这是 LLM 评测员对边界场景固有方差，不是 skill 设计硬伤
- **时间开销**：+157% 简单场景 / +41% 中途场景——任务越复杂相对开销越小
- **0→1 vs 中途接手**：中途场景下 skill 优势更"强"——禁令 #1 / 变姿态 / 复姿态在接手代码场景里几乎都能打中

## 累积演化要点（v0.1 → v0.2 → v0.3 → v0.4 description）
1. v0.1：原版，#5 与"止"内部矛盾
2. v0.2：作用域分离（"问 vs 给方案"），跳过条件 3 触发器
3. v0.3：跳过条件改成动作前置（"先写最小可运行版本，写完再问澄清"）；#5 加"补"覆盖证据收集场景
4. v0.4：description 加"加 try/except 加配置加抽象"诱惑，修复 #8 漏判

## 一句话结论
**skill 在 0→1 简单场景和中途接手场景双向都生效，价值在抑制"baseline 顺着用户假设走 / 套通用模式 / 列优化项预留"这三类反模式。本次评测证明 v0.4 final + description v2 已可正式投入使用，剩余优化点为边际递减。**
